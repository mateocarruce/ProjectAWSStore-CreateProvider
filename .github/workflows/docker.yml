name: Deploy to AWS
on:
  push:
    branches:
      - test  # Se ejecutará solo cuando hagas push a la rama 'test'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Login en Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Construir y subir imagen a Docker Hub
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/projectawsstore-createprovider:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/projectawsstore-createprovider:latest

      - name: Conectar a AWS EC2 y desplegar
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo docker stop create-provider || true
            sudo docker rm create-provider || true
            sudo docker pull ${{ secrets.DOCKER_USERNAME }}/projectawsstore-createprovider:latest
            sudo chown ec2-user:ec2-user /home/ec2-user/key.pem /home/ec2-user/cert.pem && sudo chmod 600 /home/ec2-user/key.pem /home/ec2-user/cert.pem && sudo docker stop create-provider || true && sudo docker rm create-provider || true && sudo docker pull mateocarr/projectawsstore-createprovider:latest && sudo docker run -d -p 4000:4000 -p 5000:5000 -v /home/ec2-user/key.pem:/home/ec2-user/key.pem -v /home/ec2-user/cert.pem:/home/ec2-user/cert.pem --name create-provider mateocarr/projectawsstore-createprovider:latest

